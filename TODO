Ensure that a recheck of the tables of select and maintained pool
Encrypt DB is ChaChaPoly1305
Support for joins via Structs
Add ability to insert or delete multiple databases. [Batch commands]
Ability to search by indexes

    <-- [Load all databases into memory] -->

        #![Open The Database] --> Result<Option<T>, Error>,
                    ↓
            Exists  | Doesnt Exist --> Err(e),
                ↓
        #![Recurvsive Dir Walk, Hash Compare and Load to Memory] --> Result<HashMap<Identifier, TuringFeedsDB>, Error>,
                ↓
            Exists  | Doesnt Exist -->  Err(e),
                ↓
        #![Check if a database query exists] --> Option<T>,
                ↓
            #[Check If `T` Exists]
                    ↓
                if Exists  | else Doesnt Exist --> None,
                    ↓
                Some(T)

    <-- -->
    
    <-- [SEARCHING FOR `T`] -->

        #![Open The Database] --> Result<Option<T>, Error>,
                    ↓
            Exists  | Doesnt Exist --> Err(e),
                ↓
        #![Open Table] --> Result<Option<T>, Error>,
                ↓
            Exists  | Doesnt Exist -->  Err(e),
                ↓
        #![Load Contents in HashTable hashed with SeaHash] --> Option<T>,
                ↓
            #[Check If `T` Exists]
                    ↓
                if Exists  | else Doesnt Exist --> None,
                    ↓
                Some(T)

    <-- -->
    